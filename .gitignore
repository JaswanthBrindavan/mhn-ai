# Secrets — never commit. .env.example is the committed template.
.env
.env.*
!.env.example

# Planning and assistant docs stay local; the repo holds code only.
CLAUDE.md
implementation.md
docs/
db/

# Local test documents — never commit patient data.
Sample reports/

# The drug catalogue source for the medicine_master import — 121 MB, local only.
Drug_Database

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editors / OS
.idea/
.vscode/
*.swp
Thumbs.db

# Reference screenshots of the staff THP dashboard (local only, like docs/).
Current_Thp_dashboard/

# Reference screenshots of the app's existing insight screens (local only, like docs/).
Current_Insights_Results/

# Subagent-driven-development scratch: ledger, briefs, review packages, demo seeds.
.superpowers/
