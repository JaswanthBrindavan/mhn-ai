# Runtime dependencies. Single source of truth for installs (app + Docker).
# Tool configuration lives in pyproject.toml; dependencies are NOT duplicated there.
# Pins are the versions actually resolved and tested on 2026-07-22.

fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
boto3==1.43.53
anthropic==0.117.1
pypdf==6.14.2

# Text extraction for section documents (insurance / scans / vaccinations).
# Text layer first, Tesseract OCR only for image-only pages.
# NOTE: pymupdf is AGPL-3.0 — commercial distribution needs an Artifex licence.
# NOTE: pytesseract is a binding only; the Tesseract BINARY must be installed in the
# image (Debian/Ubuntu: apt-get install -y tesseract-ocr).
pymupdf==1.26.5
pytesseract==0.3.13
pillow==11.3.0
